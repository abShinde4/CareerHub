# Frontend environment (optional)
# Dev: leave VITE_API_URL unset — requests use /api via Vite proxy.
# Production: set full API URL, e.g. https://api.yourdomain.com/api
# VITE_API_URL=

# Backend target for Vite dev proxy (must match backend PORT)
VITE_BACKEND_URL=http://localhost:5000
